

#ifndef DRONE_LINK_MANAGER_STRUCTS_H
#define DRONE_LINK_MANAGER_STRUCTS_H

// transportAddress is the transport specific device address of the TxNode, e.g. ipAddress
struct DRONE_LINK_TRANSPORT_ADDRESS {
  IPAddress ipAddress;
};

#endif